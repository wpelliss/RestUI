# RestUI — backend dependencies
# Python 3.12+
# Generé le 2026-05-30 — versions épinglées pour reproductibilité.

# Web framework + ASGI server
fastapi==0.115.12
uvicorn[standard]==0.34.2

# Templates HTML (requis pour Jinja2Templates — non bundlé dans fastapi par défaut)
jinja2==3.1.6

# HTTP client async (proxy vers API upstream)
httpx==0.28.1

# Configuration depuis .env
pydantic==2.11.5
pydantic-settings==2.9.1

# Chargement config.yaml
pyyaml==6.0.2
